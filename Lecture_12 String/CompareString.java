public class CompareString {
    public static void main(String[] args) {
        
        // compare using == operator
        /*
        string == string --> both are equal
        string == new string --> both are not equal
        
        not: == "operator" check only value of string
        "compare" method check both object.
        
        string are class so variable which is made by string is object of string.

        /*

        /*
        String s1 = "Tony";
        String s2 = "Tony";
        String s3 = new String("Tony");

        // compare s1 and s2
        if (s1 == s2) {
            System.out.println("Both are equal");
        } else {
            System.out.println("Both are not equal");
        }

        // compare s1 and s3
        if (s1 == s3) {
            System.out.println("Both are equal");
        } else {
            System.out.println("Both are not equal");
        }

        */

    }
}
