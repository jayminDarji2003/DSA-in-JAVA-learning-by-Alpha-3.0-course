// Here LinkedList --> our whole linked list . when we make object of LinkedList then we can access whole linked list. 

public class LinkedList {

    public static class Node {
        int data; // data - value we want to store
        Node next; // next - reference to the next Node and storing next node(object address)

        public Node(int data) {
            this.data = data;
            this.next = null;
        }
    }

    public static void main(String[] args) {

    }
}